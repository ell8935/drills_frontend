// import { gql, useQuery } from "@apollo/client";
export {};

// export const GET_USER_BY_EMAIL = gql`
//   query GetUserByEmail($email: String!) {
//     user(email: $email) {
//       email
//     }
//   }
// `;

// export const useUser = (email: any) => {
//   console.log(email);

//   const { data, error, loading } = useQuery(GET_USER_BY_EMAIL, { variables: { email }, skip: email === "" });

//   return { data, error, loading };
// };
