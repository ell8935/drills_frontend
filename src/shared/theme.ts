import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4589ff",
    },
    secondary: {
      main: "#515455",
    },
    error: {
      main: "#f75a5a",
    },
    success: {
      main: "#7be387",
    },
  },
});

export default theme;
